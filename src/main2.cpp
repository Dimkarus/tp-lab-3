#include <iostream>
#include <cstdio>
#include "task1.h"

int main() {
    // working in meters
//    auto radius_outer = new Circle(4), radius_inner = new Circle(3);
//    std::cout << calcCost(radius_inner, radius_outer) << std:: endl;
    printf("%lf", calcCost());
    return 0;
}
